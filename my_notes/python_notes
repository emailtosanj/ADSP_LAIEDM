#convert .ipynb extension file to .html extension file

#!pip install nbconvert
#! jupyter nbconvert --to html  'FDS_Project_LearnerNotebook_FullCode.ipynb'

#end - convert .ipynb extension file to .html extension file

#Way to check the type of a variable against different types
    # say x is 100 then
    # type(x) == float / type(x) == int

